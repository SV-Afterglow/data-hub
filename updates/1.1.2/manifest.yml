version: "1.1.2"
requires: "1.1.1"
release_date: "2024-12-21"
description: "Fixed network configuration and update service paths"

steps:
  - type: "docker_compose"
    description: "Update docker-compose.yml with fixed network configuration and paths"
    service: "all"
    action: "restart"

rollback:
  supported: true
  steps:
    - "Restore previous docker-compose.yml"
    - "Restart all services"
